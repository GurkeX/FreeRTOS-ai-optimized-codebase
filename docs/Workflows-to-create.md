## Change Board Workflow

**Status:** ✅ Implemented — see [`.github/prompts/coding/change-board.prompt.md`](../.github/prompts/coding/change-board.prompt.md)

Migrates the AI-Optimized FreeRTOS codebase to a different Pico SDK-supported board (RP2040/RP2350 ecosystem). Phases: board research → feasibility check → integration point analysis → standalone blinky verification → full codebase migration → hardware verification.

---

## Compile Release Version Workflow